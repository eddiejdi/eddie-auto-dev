name: Build & Publish Extension

on:
  push:
    branches: [main]
    paths:
      - "tools/tools/rpa4all-chrome-extension/**"
      - "tools/tools/rpa4all-form-autofill-extension-unpacked/**"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "build ok"
