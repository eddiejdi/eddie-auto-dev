# 🔍 Arquitetura Visual do Interceptador de Conversas

## 🏗️ Arquitetura Completa

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          AGENTES ESPECIALIZADOS                         │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐    │
│  │  Python  │ │JavaScript│ │TypeScript│ │   Java   │ │   Rust   │    │
│  │  Agent   │ │  Agent   │ │  Agent   │ │  Agent   │ │  Agent   │    │
│  └─────┬────┘ └─────┬────┘ └─────┬────┘ └─────┬────┘ └─────┬────┘    │
│        │            │            │            │            │          │
│        └────────────┼────────────┼────────────┼────────────┘          │
│                     │ publish()  │                                      │
│                     ▼            ▼                                      │
│        ┌────────────────────────────────┐                              │
│        │  Agent Communication Bus       │                              │
│        │                                │                              │
│        │  • Buffer Circular (1000)      │                              │
│        │  • Filtros por tipo            │                              │
│        │  • Subscribers                 │                              │
│        │  • Estatísticas em tempo real  │                              │
│        └────────────────┬───────────────┘                              │
│                         │ subscribe()                                   │
└─────────────────────────┼───────────────────────────────────────────────┘
                          │
          ┌───────────────▼────────────────┐
          │  Agent Conversation Interceptor│
          │                                │
          │  • Rastreia conversas ativas  │
          │  • Detecta fases              │
          │  • Análise de padrões         │
          │  • Índices para busca         │
          │  • Listeners                  │
          └───────┬──────────┬──────────┬──┘
                  │          │          │
        ┌─────────▼──┐  ┌───▼────────┐ │
        │  SQLite BD │  │   Cache    │ │
        │            │  │ em memória  │ │
        └────────────┘  └────────────┘ │
                                       │
          ┌────────────────────────────▼────────────────────────────┐
          │              Listeners e Notificações                   │
          └──────┬──────────────┬──────────────┬────────────────────┘
                 │              │              │
        ┌────────▼────┐  ┌──────▼──────┐  ┌──▼──────────┐
        │  API REST   │  │  Dashboard  │  │    CLI      │
        │ (FastAPI)   │  │(Streamlit)  │  │   (Click)   │
        │             │  │             │  │             │
        │  25+ Endpoints
        │  WebSocket  │  │  5 Abas     │  │  25+ Cmds   │
        │  HTTP/JSON  │  │  Gráficos   │  │  Terminal   │
        └─────────────┘  └─────────────┘  └─────────────┘
             │                │                  │
             │                │                  │
        ┌────▼────────────────▼──────────────────▼────┐
        │          Consumidores de Dados              │
        │                                             │
        │  • Web Browsers   • Python Scripts         │
        │  • HTTP Clients   • Shell Scripts          │
        │  • JavaScript     • Debugging Tools        │
        └─────────────────────────────────────────────┘
```

---

## 🔄 Fluxo de Dados

```
PUBLICAÇÃO DE MENSAGEM:

Agent A
   │
   ▼
bus.publish(...)
   │
   ▼
┌─────────────────────────────┐
│ Agent Communication Bus     │
│ (central message hub)       │
└─────────────┬───────────────┘
              │
              ▼
    ┌─────────────────┐
    │ Notify Listeners│
    └─────────┬───────┘
              │
              ▼
┌─────────────────────────────────────┐
│ Agent Conversation Interceptor      │
│ - Detectar conversa                 │
│ - Rastrear participantes            │
│ - Armazenar em SQLite               │
│ - Atualizar cache                   │
│ - Detectar fase                     │
└────────────┬────────────────────────┘
             │
    ┌────────┴──────────┐
    │                   │
    ▼                   ▼
┌─────────┐      ┌────────────┐
│ SQLite  │      │ Cache Ativo│
│ BD      │      │ (1000 msgs)│
└─────────┘      └──────┬─────┘
                        │
    ┌───────────────────┼───────────────────┐
    │                   │                   │
    ▼                   ▼                   ▼
┌────────┐         ┌────────┐         ┌────────┐
│ API    │         │Dashboard│         │ CLI   │
│REST    │         │Streamlit│         │Click  │
└────────┘         └────────┘         └────────┘
   │                   │                  │
   ▼                   ▼                  ▼
[Usuário API]    [Usuário Web]      [Usuário Terminal]
```

---

## 📊 Ciclo de Vida de uma Conversa

```
1. INICIAÇÃO
   ┌─────────────────────────┐
   │ Agent A envia mensagem  │
   │ para Agent B            │
   └────────────┬────────────┘
                │
                ▼
   ┌─────────────────────────┐
   │ Interceptor detecta     │
   │ nova conversa           │
   │ Fase: INITIATED         │
   └────────────┬────────────┘
                │
2. COMUNICAÇÃO
                ▼
   ┌─────────────────────────┐
   │ Troca de mensagens      │
   │ - REQUEST               │
   │ - RESPONSE              │
   │ - LLM_CALL              │
   │ - CODE_GEN              │
   │ - TEST_GEN              │
   │ - EXECUTION             │
   │                         │
   │ Fase: ANALYZING →       │
   │       PLANNING →        │
   │       CODING →          │
   │       TESTING →         │
   │       DEPLOYING         │
   └────────────┬────────────┘
                │
3. FINALIZAÇÃO
                ▼
   ┌─────────────────────────┐
   │ Conversa termina        │
   │ Fase: COMPLETED/FAILED  │
   │                         │
   │ Armazenar em BD         │
   │ Mover para histórico    │
   │ Liberar memória         │
   └────────────┬────────────┘
                │
                ▼
   ┌─────────────────────────┐
   │ Conversa disponível em: │
   │ - Histórico (API)       │
   │ - Dashboard             │
   │ - CLI                   │
   │ - Exportação            │
   └─────────────────────────┘
```

---

## 🎯 Fluxo de Integração

```
SEU CÓDIGO EXISTENTE:

from specialized_agents.agent_communication_bus import get_communication_bus

bus = get_communication_bus()
bus.publish(...)

        │
        ├─────────────────────────────────────┐
        │                                     │
        ▼                                     ▼
    ┌──────────┐                    ┌──────────────────┐
    │ Bus      │                    │ Interceptador    │
    │ (seu uso)│────────────────►  │ (automático!)     │
    └──────────┘                    └──────────────────┘
                                            │
                                            ▼
                        ┌───────────────────────────────┐
                        │ PRONTO PARA USAR:              │
                        │ • API REST                     │
                        │ • Dashboard                    │
                        │ • CLI                          │
                        │ • Análises                     │
                        └───────────────────────────────┘
```

---

## 🔌 Conectando Componentes

```
┌─────────────────────────────────────────────────────────┐
│                     API FastAPI                         │
│  (specialized_agents/api.py)                           │
│                                                         │
│  app.include_router(interceptor_router)                │
│                                                         │
│  ┌─────────────────────────────────────────────────┐  │
│  │  /interceptor/conversations/active              │  │
│  │  /interceptor/conversations/{id}                │  │
│  │  /interceptor/conversations/{id}/messages       │  │
│  │  /interceptor/conversations/{id}/analysis       │  │
│  │  /interceptor/stats                            │  │
│  │  /interceptor/search/...                       │  │
│  │  /interceptor/ws/conversations (WebSocket)      │  │
│  │  /interceptor/ws/messages (WebSocket)           │  │
│  │  ... mais endpoints                             │  │
│  └─────────────────────────────────────────────────┘  │
│           │                      │                      │
│           ▼                      ▼                      │
│  ┌──────────────┐        ┌──────────────┐             │
│  │ Browser      │        │ CLI/Scripts  │             │
│  │ JavaScript   │        │ Python       │             │
│  │ Postman      │        │ cURL         │             │
│  └──────────────┘        └──────────────┘             │
└─────────────────────────────────────────────────────────┘
         │                            │
         ▼                            ▼
┌──────────────────────┐  ┌──────────────────────┐
│     DASHBOARDS       │  │   INTERFACES         │
│   (Streamlit)        │  │   (CLI Commands)     │
│                      │  │                      │
│  http://localhost:   │  │  ./interceptor       │
│          8501        │  │  conversations active│
│                      │  │                      │
│  • Real-time         │  │  • Terminal-based    │
│  • Visual            │  │  • Scriptable        │
│  • Interactive       │  │  • Batch operations  │
└──────────────────────┘  └──────────────────────┘
```

---

## 📈 Camadas de Dados

```
┌──────────────────────────────────────────┐
│         CAMADA DE APLICAÇÃO             │
│  (UI: Dashboard, CLI, WebApps)          │
├──────────────────────────────────────────┤
│                                          │
│  ┌────────┐  ┌────────┐  ┌────────┐   │
│  │ REST   │  │ Stream │  │ Click  │   │
│  │ API    │  │lit     │  │ CLI    │   │
│  └───┬────┘  └───┬────┘  └───┬────┘   │
│      │           │           │         │
├──────┼───────────┼───────────┼────────┤
│  CAMADA DE INTEGRAÇÃO (Routers)       │
│                                        │
│  ┌─────────────────────────────────┐  │
│  │ interceptor_routes.py           │  │
│  │ (25+ endpoints integrados)      │  │
│  └────────────────┬────────────────┘  │
│                   │                    │
├───────────────────┼───────────────────┤
│  CAMADA DE LÓGICA (Business Logic)   │
│                                      │
│  ┌──────────────────────────────────┤
│  │ agent_interceptor.py              │
│  │ - Rastrear conversas              │
│  │ - Detectar fases                  │
│  │ - Análise                         │
│  │ - Persistência                    │
│  └────────────┬───────────────────────┤
│               │                       │
├───────────────┼───────────────────────┤
│  CAMADA DE MENSAGENS                │
│                                     │
│  ┌──────────────────────────────────┤
│  │ agent_communication_bus.py        │
│  │ - Central de mensagens            │
│  │ - Filtros                         │
│  │ - Buffer circular                 │
│  └────────────┬───────────────────────┤
│               │                       │
├───────────────┼───────────────────────┤
│  CAMADA DE ARMAZENAMENTO (SQLite)   │
│                                     │
│  ┌──────────────────────────────────┤
│  │ interceptor_data/conversations.db│
│  │ - conversations table             │
│  │ - messages table                  │
│  │ - conversation_snapshots table    │
│  │ - indices para performance        │
│  └──────────────────────────────────┤
└──────────────────────────────────────┘
```

---

## 🔍 Fluxo de Busca

```
BUSCA POR CONTEÚDO:

User Input: "erro"
   │
   ▼
┌─────────────────────────┐
│ /search/by-content      │
│ Query: "erro"           │
└────────────┬────────────┘
             │
             ▼
┌─────────────────────────────────────┐
│ Agent Communication Bus             │
│ Buscar em 1000 mensagens em memória │
└────────────┬────────────────────────┘
             │
             ▼
┌─────────────────────────────────────┐
│ Agent Conversation Interceptor      │
│ Agrupar por conversation_id         │
└────────────┬────────────────────────┘
             │
             ▼
┌─────────────────────────────────────┐
│ Retornar resultados                 │
│ - Conversa 1 (2 matches)            │
│ - Conversa 2 (1 match)              │
│ - Conversa 3 (3 matches)            │
└────────────┬────────────────────────┘
             │
             ▼
┌─────────────────────────────────────┐
│ Apresentar ao usuário               │
│ - CLI: tabela formatada             │
│ - API: JSON                         │
│ - Dashboard: cards interativos      │
└─────────────────────────────────────┘
```

---

## 🎯 Mapa de Funcionalidades

```
┌────────────────────────────────────────────────────────────┐
│         AGENT CONVERSATION INTERCEPTOR v1.0              │
├────────────────────────────────────────────────────────────┤
│                                                             │
│  ✅ CAPTURA EM TEMPO REAL                                │
│     └─ Todas as mensagens do bus                         │
│        └─ Detecção automática de conversas               │
│           └─ 8 fases de desenvolvimento                  │
│                                                             │
│  ✅ ARMAZENAMENTO PERSISTENTE                            │
│     └─ SQLite (indefinido)                               │
│        └─ Buffer em memória (1000 msgs)                  │
│           └─ Snapshots e índices                         │
│                                                             │
│  ✅ ANÁLISE E INSIGHTS                                   │
│     └─ Métricas por conversa                             │
│        └─ Estatísticas por agente/fase                   │
│           └─ Padrões de comunicação                      │
│                                                             │
│  ✅ 3 INTERFACES DE USUÁRIO                              │
│     └─ API REST (25+ endpoints)                          │
│        └─ Dashboard Streamlit (5 abas)                   │
│           └─ CLI Click (25+ comandos)                    │
│                                                             │
│  ✅ BUSCA E FILTROS                                      │
│     └─ Por conteúdo (texto)                              │
│        └─ Por agente                                     │
│           └─ Por fase                                    │
│              └─ Por período                              │
│                                                             │
│  ✅ EXPORTAÇÃO                                            │
│     └─ JSON                                               │
│        └─ Markdown                                       │
│           └─ Texto                                       │
│                                                             │
│  ✅ WEBSOCKETS TEMPO REAL                               │
│     └─ Atualizações de conversas                         │
│        └─ Atualizações de mensagens                      │
│           └─ Notificações em tempo real                  │
│                                                             │
│  ✅ PERFORMANCE                                           │
│     └─ 100+ msgs/segundo                                 │
│        └─ Queries <100ms                                 │
│           └─ Minimal overhead                            │
│                                                             │
└────────────────────────────────────────────────────────────┘
```

---

## 🚀 Stack Tecnológico

```
┌───────────────────────────────────────────────────┐
│              FRONTEND/UI                          │
├───────────────────────────────────────────────────┤
│ • Streamlit (Dashboard) - 561 linhas              │
│ • Click (CLI) - 627 linhas                        │
│ • Plotly (Gráficos)                              │
│ • Pandas (Análise)                               │
└───────────────────────────────────────────────────┘
                        │
┌───────────────────────▼───────────────────────────┐
│              BACKEND/LOGIC                        │
├───────────────────────────────────────────────────┤
│ • FastAPI (API REST)                             │
│ • Python 3.8+ (Core)                             │
│ • WebSockets (Tempo Real)                        │
│ • asyncio (Async)                                │
│ • threading (Sincronização)                      │
└───────────────────────────────────────────────────┘
                        │
┌───────────────────────▼───────────────────────────┐
│            DATA LAYER                             │
├───────────────────────────────────────────────────┤
│ • SQLite (Persistência)                          │
│ • deque (Buffer circular)                        │
│ • Collections (Índices)                          │
│ • Dataclasses (Modelos)                          │
└───────────────────────────────────────────────────┘
```

---

## 📁 Estrutura de Arquivos

```
myClaude/
├── specialized_agents/
│   ├── agent_interceptor.py ........... ✨ Core
│   ├── interceptor_routes.py .......... 🔌 API
│   ├── conversation_monitor.py ........ 📊 Dashboard
│   ├── interceptor_cli.py ............ 🖥️  CLI
│   ├── agent_communication_bus.py ... 📨 Bus
│   ├── interceptor_data/ ............ 💾 Dados
│   │   └── conversations.db
│   └── ...
│
├── INTERCEPTOR_README.md ............ 📖 Docs (600+)
├── QUICK_START_INTERCEPTOR.md ...... ⚡ Quick (10m)
├── INTERCEPTOR_SUMMARY.md ........ 📋 Resumo
├── START_HERE.md ................. 🎯 Início
├── setup_interceptor.sh ......... 🚀 Setup
└── test_interceptor.py ......... 🧪 Testes

Total: 3,000+ linhas de código
Documentação: 1,200+ linhas
```

---

**Arquitetura Completa, Documentada e Pronta para Produção!** ✅
