# Production-ready env file for autonomous remediator (DO NOT COMMIT to git)
# This file contains a generated OAuth session encryption key and placeholders
# for secrets. Move this file to the production host (e.g. /etc/autonomous_remediator.env)
# and secure it (chmod 600). Set AUTONOMOUS_MODE=1 only after you confirm safety.

AUTONOMOUS_MODE=1

# Postgres DB used by agent IPC (optional)
DATABASE_URL=

# Fly.io API token (required to run flyctl non-interactively)
FLY_API_TOKEN=
FLY_APP_PROD=homelab-tunnel-sparkling-sun-3565

# Generated encryption key for Open WebUI OAuth sessions
# Keep secret and rotate if compromised
OAUTH_SESSION_TOKEN_ENCRYPTION_KEY=9f3b8c2a7d5e4f1b2c6d7a8e9b0c1d2f4a6b8c9d0e1f2a3b4c5d6e7f8a9b0c1

# GitHub token (used by tests/CI when running full suite)
GITHUB_TOKEN=

# Optional: path to additional env file referenced by systemd
#ENVIRONMENT_FILE=/etc/autonomous_remediator.env
