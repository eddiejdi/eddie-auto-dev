# Production environment template for autonomous remediator
# Copy this file to a secure location on the target host and fill values
# Example: /etc/autonomous_remediator.env
# DO NOT commit secrets to git.

# Set to 1 to allow real remediation actions. Keep 0 for dry-run.
AUTONOMOUS_MODE=0

# Postgres DB used by agent IPC (optional for DB fallback)
# Example: postgresql://postgres:password@db-host:5432/autocoinbot
DATABASE_URL=

# Fly.io API token (required to run flyctl non-interactively)
# Fly app name to operate on (production app)
FLY_APP_PROD=homelab-tunnel-sparkling-sun-3565

# Encryption key used by Open WebUI for OAuth sessions.
# Generate a 32+ char random value and keep it secret.
OAUTH_SESSION_TOKEN_ENCRYPTION_KEY=

# GitHub token (used by some tests and CI steps)
GITHUB_TOKEN=

# Optional: path to additional env file(s) referenced by systemd unit
#ENVIRONMENT_FILE=/etc/autonomous_remediator.env
FLY_API_TOKEN=REPLACE_WITH_REAL_FLY_API_TOKEN
