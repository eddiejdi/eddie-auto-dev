# KICKOFF - 2026-01-21

Equipe,

Estou iniciando oficialmente o trabalho na instância homelab. Abaixo as prioridades imediatas e como reportar progresso.

Prioridades iniciais

1. Investigar `btc-daily-report.service`
   - Sintoma: serviço oneshot sai com exit-code 1 ao tentar enviar relatório via WhatsApp.
   - Logs: `/home/homelab/myClaude/btc_trading_agent/logs/daily_report.log` mostram erros: `401 Unauthorized`, `Connection refused` (localhost:3000), `405 Method Not Allowed`.
   - Ações: verificar se o serviço WhatsApp local (porta 3000) está ativo; validar credenciais/tokens; executar manualmente o script para capturar traceback.

2. Verificar infra de mensagens e monitoramento
   - Streamlit monitor: `http://192.168.15.2:8501` (use sidebar "Interceptor API" para apontar `http://192.168.15.2:8503`)
   - Confirme que as mensagens em tempo real aparecem. Use o botão "Send interceptor test message" na sidebar para testar o pipeline.

3. Validar mudanças no Copilot/homelab client
   - Arquivo: `eddie-copilot/src/homelabClient.ts`
   - Testes: reproduzir geração de código via AgentManager (`POST /code/generate`) e revisar logs.

4. Priorizar bugs e melhorias
   - Ver `docs/TEAM_BACKLOG.md` e abrir issues para itens que exigem PRs ou investigação adicional.

Comunicação e rastreio

- Use o barramento de comunicação (bus) — as mensagens são visíveis via `http://192.168.15.2:8503/interceptor/conversations/active` e WS `ws://192.168.15.2:8503/interceptor/ws/messages`.
- Publique atualizações de progresso no bus com `MessageType.COORDINATOR` (o endpoint `/communication/test` envia via coordenador).

Ações que já fiz

- Registrei uma mensagem de kickoff no barramento (coordenador) e notifiquei o Diretor (msg id enviado).
- Configurei o Streamlit monitor com override de `INTERCEPTOR_API` e adicionei botão de teste.

Próximos passos (para a equipe)

- (Ops) Verificar e restaurar serviço WhatsApp local.
- (Dev) Reproduzir falha do `btc-daily-report.py` e criar PR com correção (ou abrir issue com traceback).
- (QA) Validar geração de código do AgentManager e aprovar mudanças no `eddie-copilot`.
- (Coord) Reportar status no barramento a cada mudança importante.

Se precisar, eu executo comandos de diagnóstico ou abro issues automaticamente.

— Coordenador automático
