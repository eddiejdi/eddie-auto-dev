# Eddie Tray Agent — Dependencies
# Core
httpx>=0.24.0
pystray>=0.19.0
Pillow>=10.0.0

# Voice recognition
SpeechRecognition>=3.10.0
PyAudio>=0.2.14          # Necessário para microfone

# Linux D-Bus (screen lock detection)
dbus-python>=1.3.2; sys_platform == "linux"
PyGObject>=3.42.0; sys_platform == "linux"

# Offline voice recognition (opcional)
# vosk>=0.3.45
