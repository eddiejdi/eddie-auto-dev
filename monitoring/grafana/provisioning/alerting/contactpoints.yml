# Grafana Unified Alerting — Contact Points (Telegram)
# Provisionado automaticamente pelo shared-auto-dev
# Docs: https://grafana.com/docs/grafana/latest/alerting/set-up/provision-alerting-resources/file-provisioning/
apiVersion: 1

contactPoints:
  - orgId: 1
    name: eddie-telegram
    receivers:
      - uid: eddie-telegram-receiver
        type: telegram
        disableResolveMessage: false
        settings:
          # Interpolado pelo Grafana a partir do ambiente do container.
          # O valor literal que estava aqui vazou neste repo público desde
          # 2026-03-04 e já foi revogado — este contact point não entrega nada
          # até que TELEGRAM_BOT_TOKEN seja injetado no container do Grafana.
          # Ver docs/INCIDENTS/2026-07-28_SECRETS_IN_PUBLIC_REPO.md.
          bottoken: "$TELEGRAM_BOT_TOKEN"
          chatid: "948686300"
          disable_web_page_preview: false
          protect_content: false
          message: |
            {{ if eq .Status "firing" }}🔴{{ else }}🟢{{ end }} {{ .Status | toUpper }}
            
            {{ range .Alerts }}
            {{ .Labels.alertname }}
            Severidade: {{ .Labels.severity }}
            {{ if .Labels.profile }}Perfil: {{ .Labels.profile }}{{ end }}
            {{ if .Annotations.summary }}📋 {{ .Annotations.summary }}{{ end }}
            {{ if .Annotations.description }}📝 {{ .Annotations.description }}{{ end }}
            {{ if .Values }}Valor: {{ .Values }}{{ end }}
            {{ end }}
  - orgId: 1
    name: eddie-ltfs-webhook
    receivers:
      - uid: eddie-ltfs-webhook-receiver
        type: webhook
        disableResolveMessage: false
        settings:
          url: "http://192.168.15.2:5001/alerts"
          httpMethod: POST
          maxAlerts: "0"
