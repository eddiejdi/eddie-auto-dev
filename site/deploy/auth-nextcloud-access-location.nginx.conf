# Incluir este bloco dentro do server de auth.rpa4all.com, antes do location / final.

location = /nextcloud-access {
    return 301 /nextcloud-access/;
}

location ^~ /nextcloud-access/ {
    auth_request /outpost.goauthentik.io/auth/nginx;
    error_page 401 = @goauthentik_proxy_signin;

    proxy_pass http://127.0.0.1:8503/nextcloud-access/;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto https;
    proxy_set_header X-Forwarded-Host $host;
    proxy_set_header X-Forwarded-Prefix /nextcloud-access;
    proxy_set_header Connection "";
    proxy_buffering off;
    proxy_read_timeout 300s;
    proxy_connect_timeout 15s;
}
