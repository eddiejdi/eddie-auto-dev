#!/usr/bin/env bash
echo "Fly.io token injection removed — this helper is disabled."
echo "If you need to provision tunnel tokens, use your secure vault workflow and avoid storing tokens in the repository."
exit 0
