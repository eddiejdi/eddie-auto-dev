#!/usr/bin/env bash
# Fly.io removed — replicate_openwebui_prod.sh is disabled to avoid accidental flyctl operations.
# If you need to re-enable Fly-related deployment automation, restore this file from version control.

echo "Fly.io references removed — replicate_openwebui_prod.sh is disabled."
exit 0
