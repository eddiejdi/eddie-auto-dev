### Example environment file for agent-network-exporter systemd unit
# Copy this file to /etc/default/agent-network-exporter on the target host
# and make sure it is owned by root and mode 600. DO NOT commit real
# credentials to the repository.
#
# Example usage on the host:
#   sudo cp tools/systemd/agent-network-exporter.env.example /etc/default/agent-network-exporter
#   sudo chmod 600 /etc/default/agent-network-exporter
#   sudo systemctl daemon-reload
#   sudo systemctl restart agent-network-exporter

# Postgres connection string used by the exporter and interceptor.
# Provide this from your secrets manager (Bitwarden) or host environment.
DATABASE_URL=postgresql://postgres:eddie_memory_2026@192.168.15.2:5432/postgres

# Metrics port (optional override)
AGENT_NETWORK_EXPORTER_PORT=9101

# Keep Python output unbuffered for logging
PYTHONUNBUFFERED=1

# Optional: any other env vars required by the service
#EXTRA_VAR=valor
